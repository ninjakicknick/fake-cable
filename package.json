{
  "name": "fake-cable",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --check app.js && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
